RSpec.describe I3WorkspaceAgnosticOpen do
  it "has a version number" do
    expect(I3WorkspaceAgnosticOpen::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
